ARK Scraper Collector has been deployed as a CronJob.

Schedule: {{ .Values.cronjob.schedule }}

To check the CronJob status:
  kubectl get cronjob {{ include "ark-scraper-collector.fullname" . }} -n {{ .Release.Namespace }}

To view recent job runs:
  kubectl get jobs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "ark-scraper-collector.name" . }}
